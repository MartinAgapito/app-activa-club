# docs/api

Contratos de API (Amazon API Gateway / Lambda) del proyecto Activa Club.

Los contratos deben documentarse aquí antes de implementar la integración
entre `apps/web` y `apps/api` (ver `docs/product/contexto-maestro.md`, sección
"Normas de ingeniería").
